# Scripts

Repository command dispatch and verification scripts for `sdkwork-doudizhu`.

- `sdkwork-command.mjs`: standard `dev`, `build`, `test`, `check`, `clean` dispatcher
- `sdkwork-verify.mjs`: merge-ready verification aggregate
- `doudizhu-*.test.mjs`: standards and naming contract tests

Run `pnpm verify` from the repository root.
