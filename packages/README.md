# Packages

Shared TypeScript contracts consumed across the doudizhu workspace.

| Package | Purpose |
| --- | --- |
| `packages/common/doudizhu/sdkwork-doudizhu-contracts` | Cross-surface DTOs and helpers using `@sdkwork/utils` |

Architecture-local packages for PC UI live under `apps/sdkwork-doudizhu-pc/packages/`.
